<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>FarmStand Products</title>
  </head>
  <body>
    <h1>Welcome to Farm Stand products 🤩</h1>
    <ul>
      <% for (let product of products) { %>
      <!-- <li><%= product.name %></li> -->
      <li><a href="/products/<%= product._id %>"><%= product.name %></a></li>
      <% } %>
    </ul>
  </body>
</html>
